<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register</title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --soft-pink: rgb(240, 176, 189);
      --deep-pink: rgb(244, 62, 92);
      --light-bg: #fff7fa;
      --text-dark: #060606;
      --text-light: #777;
      --white: #fff;
    }

    body {
      font-family: 'Inter', sans-serif;
      color: var(--text-dark);
      background: linear-gradient(135deg, #ffe6f1 0%, #fff9fb 100%);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      overflow-x: hidden;
      display: flex;
      flex-direction: column;
    }

    nav {
      background: var(--deep-pink);
      color: #fff;
      box-shadow: 0 4px 12px rgba(244, 62, 92, 0.25);
      text-align: center;
      padding: 1rem 0;
      letter-spacing: 0.8px;
    }

    nav h1 {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      /* font-weight: 300; */
      padding: 0%;
      margin: 0;
      color: #fff;
      text-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    }

    .register-container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      position: relative;
      overflow: hidden;
    }

    .floating-orb {
      position: absolute;
      border-radius: 50%;
      opacity: 0.3;
      filter: blur(60px);
      animation: floatOrb 8s ease-in-out infinite alternate;
    }

    .orb2 {
      background: var(--soft-pink);
      width: 220px;
      height: 220px;
      bottom: 15%;
      right: 8%;
    }

    @keyframes floatOrb {
      from { transform: translateY(0); }
      to { transform: translateY(-30px); }
    }

    .register-card {
      position: relative;
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(14px);
      border: 1px solid rgba(246, 164, 202, 0.3);
      padding: 3rem 3rem;
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(244, 62, 92, 0.15);
      width: 375px;
      text-align: center;
      z-index: 2;
      animation: floatCard 4s ease-in-out infinite;
    }

    @keyframes floatCard {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-8px); }
    }

    .register-card h2 {
      color: var(--deep-pink);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .register-card p {
      color: var(--text-light);
      font-size: 0.95rem;
      margin-bottom: 1.8rem;
    }

    .form-control {
      width: 100%;
      padding: 0.9rem;
      margin-bottom: 1rem;
      border: 1px solid #f7c9d5;
      border-radius: 10px;
      font-size: 1rem;
      outline: none;
      background: rgba(255,255,255,0.85);
      transition: all 0.3s ease;
      box-sizing: border-box;
    }

    .form-control:focus {
      border-color: var(--deep-pink);
      box-shadow: 0 0 8px rgba(244, 62, 92, 0.3);
      background: #fff;
    }

    .password-wrapper {
      position: relative;
    }

    .toggle-password {
      position: absolute;
      top: 50%;
      right: 14px;
      transform: translateY(-50%);
      cursor: pointer;
      font-size: 0.9rem;
      color: var(--deep-pink);
      font-weight: 600;
      background: none;
      border: none;
      outline: none;
      padding-bottom: 15px;
      text-decoration: underline;
    }

    .toggle-password:hover {
      color: var(--soft-pink);
    }

    .btn-gradient {
      width: 100%;
      background: linear-gradient(90deg, var(--soft-pink), var(--deep-pink));
      border: none;
      color: white;
      border-radius: 50px;
      padding: 0.9rem;
      font-weight: 600;
      font-size: 1rem;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
      cursor: pointer;
      box-shadow: 0 8px 20px rgba(248, 6, 119, 0.25);
    }

    .btn-gradient:hover {
      background: linear-gradient(90deg, var(--deep-pink), var(--soft-pink));
      transform: translateY(-3px);
      box-shadow: 0 10px 24px rgba(248, 6, 119, 0.35);
    }

    .error {
      color: var(--deep-pink);
      font-weight: 600;
      margin-bottom: 1rem;
    }

    .login-link {
      display: block;
      margin-top: 1.5rem;
      color: var(--deep-pink);
      font-weight: 500;
      text-decoration: none;
      font-size: 0.9rem;
      transition: color 0.3s;
    }

    .login-link:hover {
      color: var(--soft-pink);
    }
  </style>
</head>
<body>

  <nav>
    <h1>Smart Expense Splitter</h1>
  </nav>

  <div class="register-container">
    <div class="floating-orb orb2"></div>

    <div class="register-card">
      <h2>Create Account</h2>
      <p>Join the crew — split smarter, spend happier!</p>

      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <form method="POST" action="/register">
        <input class="form-control" type="text" name="username" placeholder="Username" required />
        <input class="form-control" type="email" name="email" placeholder="Email" required />

        <div class="password-wrapper">
          <input
            id="password"
            class="form-control"
            type="password"
            name="password"
            placeholder="Password"
            required
            pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$"
            title="Must be 8+ characters, include uppercase, lowercase, number, and special character."
          />
          <button type="button" id="togglePassword" class="toggle-password">Show</button>
        </div>

        <button class="btn-gradient" type="submit">Sign Up</button>
      </form>

      <a class="login-link" href="/login">Already have an account? Log in</a>
    </div>
  </div>

  <script>
    const toggleBtn = document.getElementById('togglePassword');
    const passwordInput = document.getElementById('password');

    toggleBtn.addEventListener('click', () => {
      const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
      passwordInput.setAttribute('type', type);
      toggleBtn.textContent = type === 'password' ? 'Show' : 'Hide';
    });
  </script>

</body>
</html>
