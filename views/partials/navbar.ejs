<nav class="navbar navbar-expand-lg py-3">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Smart Expense Splitter</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item">
          <a class="nav-link <%= (page === 'home') ? 'active' : '' %>" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (page === 'groups') ? 'active' : '' %>" href="/groups">Groups</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (page === 'expenses') ? 'active' : '' %>" href="/expenses">Expenses</a>
        </li>

        <% /* show profile / login based on userId */ %>
        <% if (typeof userId !== 'undefined' && userId) { %>
          <li class="nav-item dropdown ms-3">
            <a class="nav-link dropdown-toggle" href="#" id="profileDropdownToggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% /* hide name: show avatar only if available */ %>
              <% if (user && user.profilePic) { %>
                <img src="/uploads/<%= user.profilePic %>" alt="avatar" class="rounded-circle" style="width:28px;height:28px;object-fit:cover;">
              <% } else { %>
                <span class="bi-person-circle" style="font-size:1.15rem;"></span>
              <% } %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdownToggle">
              <li><a class="dropdown-item" href="/profile">My Profile</a></li>
              <li><a class="dropdown-item" href="/settings">Settings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <form id="logoutForm" action="/logout" method="POST" class="m-0">
                  <button class="dropdown-item" type="submit">Logout</button>
                </form>
              </li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item ms-3"><a class="nav-link" href="/login">Login</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
